#include "version.h"

#include <stddef.h> // NULL

const char *VERSION = "@ext_VERSION@";

const char *GIT_COMMIT_SHA1 = @GIT_COMMIT_SHA1@;
const char *GIT_COMMIT_DATE = @GIT_COMMIT_DATE@;
const char *GIT_COMMIT_MESSAGE = @GIT_COMMIT_MESSAGE@;
